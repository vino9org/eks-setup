API_HOST=$(kubectl get service fund-transfer -n vinobank --output jsonpath='{.status.loadBalancer.ingress[0].hostname}')
curl -s -H "content-type: application/json" -d @transfer_1.json $API_HOST:8080/transfers -v | jq .
